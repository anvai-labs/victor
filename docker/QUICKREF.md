# Docker quick reference

Use the [Docker deployment guide](README.md) for current build and offline-test
commands, and the [MCP guide](mcp-server/README.md) for standard I/O configuration.
The [container target table](../docs/development/dependencies.md#container-targets)
explains which dependencies and entrypoint each image includes.
