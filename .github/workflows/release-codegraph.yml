# Release workflow for victor-codegraph — independent of victor-ai / victor-contracts.
#
# Triggers:
#   - Push tag: victor-codegraph-v*  (e.g. victor-codegraph-v0.1.0)
#   - Manual dispatch (optionally TestPyPI only)
#
# Publishing uses PyPI Trusted Publishing (OIDC) — NO API token secret required.
# PyPI is the authoritative publish (gated on a real victor-codegraph-v* tag). TestPyPI
# is ADVISORY/OPTIONAL: it attempts on every tag/dispatch but is `continue-on-error`, so a
# missing TestPyPI publisher never blocks the PyPI release (publish-pypi needs only `build`).
# The TestPyPI publisher config below is therefore optional — skip it to ship to PyPI only.
# To configure on the PyPI side (one-time):
#   1. Create the project owner / pending publisher at https://pypi.org/manage/account/publishing/
#   2. Add a "GitHub" trusted publisher:
#        owner   = vjsingh1984
#        repo    = victor
#        workflow= release-codegraph.yml
#        environment = pypi   (and `testpypi` for the TestPyPI publisher)
#   3. (No secrets to add — OIDC handles auth via `permissions: id-token: write`.)

name: Release codegraph

on:
  push:
    tags:
      - 'victor-codegraph-v*'
  workflow_dispatch:
    inputs:
      publish_testpypi:
        description: 'Publish to TestPyPI only'
        required: false
        default: false
        type: boolean

env:
  PYTHON_VERSION: '3.12'

jobs:
  test:
    name: Test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ matrix.python-version }}
      - name: Editable install
        run: |
          python -m pip install --upgrade pip
          pip install -e ./victor-contracts
          pip install -e "./victor-codegraph[dev]"
      - name: Test
        working-directory: victor-codegraph
        run: pytest tests -v --tb=short

  build:
    name: Build package
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Verify tag matches package version
        if: startsWith(github.ref, 'refs/tags/victor-codegraph-v')
        run: |
          python -c 'import os, pathlib, tomllib; version=tomllib.loads(pathlib.Path("victor-codegraph/pyproject.toml").read_text())["project"]["version"]; tag=os.environ["GITHUB_REF_NAME"].removeprefix("victor-codegraph-v"); assert tag == version, f"tag {tag} != package {version}"'
      - name: Install build tools
        run: pip install build twine
      - name: Build sdist + wheel
        working-directory: victor-codegraph
        run: python -m build
      - name: Check metadata
        run: twine check victor-codegraph/dist/*
      - name: Upload artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: codegraph-dist
          path: victor-codegraph/dist/

  publish-testpypi:
    name: Publish to TestPyPI (advisory)
    needs: build
    # Advisory/optional: attempts on every tag + dispatch, but never blocks the release.
    # If no TestPyPI trusted publisher is configured, this job fails softly (continue-on-error)
    # and the PyPI publish below — which does not depend on it — still proceeds.
    if: ${{ startsWith(github.ref, 'refs/tags/victor-codegraph-v') || github.event_name == 'workflow_dispatch' }}
    continue-on-error: true
    runs-on: ubuntu-latest
    environment:
      name: testpypi
      url: https://test.pypi.org/project/victor-codegraph/
    permissions:
      id-token: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codegraph-dist
          path: dist/
      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        continue-on-error: true
        with:
          repository-url: https://test.pypi.org/legacy/
          skip-existing: true

  publish-pypi:
    name: Publish to PyPI
    needs: build
    if: startsWith(github.ref, 'refs/tags/victor-codegraph-v') && !contains(github.ref, 'alpha') && !contains(github.ref, 'beta') && !contains(github.ref, 'rc')
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/project/victor-codegraph/
    permissions:
      id-token: write
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: codegraph-dist
          path: dist/
      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          verbose: true
          skip-existing: true
